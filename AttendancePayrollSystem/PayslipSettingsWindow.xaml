<Window x:Class="AttendancePayrollSystem.PayslipSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Payslip Settings"
        Height="260"
        Width="460"
        WindowStartupLocation="CenterOwner"
        MinHeight="220" MinWidth="360"
        Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="22">
        <Border Style="{StaticResource ModernCard}" Padding="22">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="Payslip Settings" Style="{StaticResource HeaderText}" Margin="0,0,0,4" />
                    <TextBlock Text="These values appear in the Certified By section of printed payslips."
                               Foreground="#64748B"
                               TextWrapping="Wrap" />
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,0,0,10">
                    <TextBlock Text="Certifying Officer Name" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="OfficerNameTextBox" Style="{StaticResource ModernTextBox}" />
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,10">
                    <TextBlock Text="Certifying Officer Title / Designation" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="OfficerTitleTextBox" Style="{StaticResource ModernTextBox}" />
                </StackPanel>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Save"
                            Width="100"
                            Margin="0,0,8,0"
                            Style="{StaticResource ModernButton}"
                            Click="Save_Click" />
                    <Button Content="Cancel"
                            Width="100"
                            Style="{StaticResource ModernButton}"
                            Click="Cancel_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
